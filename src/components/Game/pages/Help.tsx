import React, { FC } from "react";
import { Panel } from "../sideNav/Panel";

const Help: FC = () =>{
    return(
        <Panel header={"Markets"} onClick={() =>{}} >
                
        </Panel>
    )
}

export default Help